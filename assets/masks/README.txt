Optional masks for NRCU FX Lab v0.2.

EDGE MASK is used only when Edge Source = CUSTOM EDGE MASK.
TREATMENT MASK limits Base/RGB/Dither/Fringe coverage and is independent of the edge mask.
Use grayscale or alpha-readable PNG/WebP files.
